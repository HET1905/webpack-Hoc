import React from "react";
import Button from "./components/Button";
const app = () => {
  return (
    <div>
      <h1>App file</h1>
      <Button disabled />
      <Button />
    </div>
  );
};

export default app;
